{
  "name": "SyncSession",
  "type": "object",
  "properties": {
    "code": {
      "type": "string",
      "description": "6-character pairing code shown on the TV"
    },
    "channel": {
      "type": "object",
      "additionalProperties": true,
      "description": "Channel the phone sent to the TV (name, url, logo, group)"
    }
  },
  "required": [
    "code"
  ]
}